epochs: 20
batch_size: 8
learning_rate: 0.001
weight_decay: 1.0e-6
optimizer: adam
scheduler:
  name: cosine
  t_max: 20
  min_lr: 1.0e-5
mixed_precision: false
grad_clip_norm: 1.0
log_every_n_steps: 10
checkpoint_every_n_epochs: 1
early_stopping_patience: 10
resume_from: null

